# import cherrypy
# from flora import DevicesController

# dispatcher = cherrypy.dispatch.RoutesDispatcher()

# # /nodes (GET)
# dispatcher.connect(name='devices',
#                     route='/v1/devices',
#                     action='get_devices',
#                     controller=DevicesController(),
#                     conditions={'method': ['GET']})
